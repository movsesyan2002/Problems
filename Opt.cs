namespace Enumarate {
    public enum CompareOption
    {
        CaseSensitive,    // Default character-by-character comparison
        CaseInsensitive,  // Ignores case differences
        ReverseOrder,     // Compares strings in reverse order
        WhiteSpaces
    }   

}

